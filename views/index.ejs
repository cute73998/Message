<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="/images/download(4).png">
</head>

<body>
    <div class="container">
        <img src="/images/j97.png" alt="Logo" class="mx-auto d-block" style="width:150px;height:150px;">
    </div>
    <div class="text-center">
        <h1 class="text-primary">Thằng nào thấy web này đều bị Gayyyyy!</h1>
        <h3 class="text-success">hẹ hẹ hẹ hẹ</h3>
    </div>
    <div class="container mt-5">
        <table class="table table-striped table-boldered table-hover">
            <thead class="table-primary">
                <tr>
                    <th><strong>Text</strong></th>
                    <th><strong>User</strong></th>
                    <th><strong>Date</strong></th>
                    <th><strong>Function</strong></th>
                </tr>
            </thead>
            <tbody>
                <% if(messages.length> 0){ %>
                    <% messages.map((m, index)=> { %>
                        <tr>
                            <td>
                                <%= m.text %>
                            </td>
                            <td>
                                <%= m.user %>
                            </td>
                            <td>
                                <%= new Date(m.added).toLocaleDateString() %>
                            </td>
                            <td>
                                <button class="btn btn-danger"><a class="text-light list-unstyled"
                                        href="/delete/<%= index %>">Delete</a></button>
                            </td>
                        </tr>
                        <%})%>
                            <% } else { %>
                                <tr>
                                    <h4>Not found any text. Please Add more text</h4>
                                </tr>
                                <%}%>
            </tbody>
        </table>
        <div>
            <button class="btn btn-success"><a href="/new" class="text-light">Add New Text</a></button>
        </div>
    </div>
</body>

</html>